apiVersion: v1
kind: Service
metadata:
  name: shap-explainer-service
  namespace: fraud-detection
spec:
  type: ClusterIP
  selector:
    app: shap-explainer
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
