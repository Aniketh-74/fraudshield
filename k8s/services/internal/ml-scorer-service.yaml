apiVersion: v1
kind: Service
metadata:
  name: ml-scorer-service
  namespace: fraud-detection
spec:
  type: ClusterIP
  selector:
    app: ml-scorer
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
